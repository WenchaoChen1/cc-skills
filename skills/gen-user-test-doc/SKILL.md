---
name: gen-user-test-doc
description: 根据需求文档和设计文档生成供测试人员点击验证的手动测试文档
tags: [testing, documentation, qa]
version: 1.0.0
author: Wenchao Chen
---

> **路径变量**：路径 = `{根变量}/{可配变量}/固定后缀`。根变量和可配变量均可在 `cc-skills.json` 中自定义，仅后缀固定。详见 `config/README.md`。

# 生成手动测试文档

根据 `{features}/{name}/` 下的需求文档和设计文档，生成供测试人员点击验证的手动测试文档。
文档包含功能测试步骤、边界/异常场景、UI 交互验证和验收检查清单四个部分。

## 使用方式

```
/gen-user-test-doc <功能名称 或 文档路径>
```

例如：
- `/gen-user-test-doc financial-dashboard` — 按功能名称，自动读取 `{features}/` 下对应目录
- `/gen-user-test-doc 财务看板`
- `/gen-user-test-doc path/to/design-doc.md path/to/requirement.md` — 直接指定文档路径

> 传入功能名称时，自动读取 `{features}/{name}/requirement/` 和 `{features}/{name}/dev-design/` 目录
> 传入文件路径时，直接读取指定文件，不依赖 `{features}/` 目录结构

---

## 执行步骤

### 第一步：读取材料

1. 若 `$ARGUMENTS` 非空，先判断参数类型：
   - **情况 A — 指定了文档路径**（参数含文件扩展名如 `.md`、`.docx`、`.txt`，或指向已存在的文件路径）：
     - 直接读取指定文件作为输入材料，**不扫描 `{features}/` 固定目录**
     - 支持多个文件路径（空格分隔），依次读取
     - 测试文档保存到文档所在目录的同级 `user-test/` 下
     - **跳过下方第 3、4 条**（目录扫描和 `{features}/` 路径的重复检查均不执行）
     - 保存前自行检查输出路径下是否已有同名文件，若已存在则按第 3 条相同逻辑提示用户选择
   - **情况 B — 仅指定功能名称**（参数不含文件扩展名，如 `xxx` 或 `{features}/xxx`）：
     - 解析功能名称，继续执行下方第 3、4 条
2. 若 `$ARGUMENTS` 为空：
   - 扫描 `{features}/` 目录，列出所有子目录
   - 若只有一个：提示「检测到 `{features}/{name}/`，是否生成该功能的测试文档？[Y/n]」
   - 若有多个：列出编号清单，提示用户选择
   - 若无匹配：提示「未找到功能目录，请提供功能名称，例如：/gen-user-test-doc my-feature」
3. **仅情况 B 和空参数时执行 — 重复创建检查**：若 `{features}/{name}/user-test/user-test-doc.md` 已存在：
   ```
   ⚠️ 文件已存在：{features}/{name}/user-test/user-test-doc.md
   选项：A. 覆盖（原文件被替换）  B. 取消（默认）  C. 查看现有文件内容
   ```
   等待用户选择，默认取消。
4. **仅情况 B 和空参数时执行** — 读取以下目录下所有文件（逐一读取）：
   - `{features}/{name}/requirement/`（功能清单、业务规则、边界条件）
   - `{features}/{name}/dev-design/`（UI 交互行为、接口规格、异常处理）
   **文件读取容错**：
   - `requirement/` 目录不存在 → 提示「缺少需求文档，请先运行 `/gen-requirement-doc <功能名称>`」，不继续后续步骤
   - `dev-design/` 目录不存在 → 提示「缺少设计文档，请先运行 `/dev/gen-design-doc <功能名称>`」，不继续后续步骤
   - 文件存在但内容为空 → 提示「文件存在但内容为空，请检查对应文件」
   - 编码问题（乱码） → 提示「文件编码异常，建议重新保存为 UTF-8」

### 第二步：整理测试范围

从读取的文档中提取：

- **功能点清单**：来自 `requirement-doc.md` 第 3 章，按 P0/P1/P2 优先级排列
- **业务流程**：来自 `requirement-doc.md` 第 4 章，每条主流程和分支流程
- **边界条件**：来自 `requirement-doc.md` 第 5.4 章，所有异常场景
- **交互行为**：来自 `dev-design-doc.md` 第 2.4 章，每条触发元素和效果
- **空态/加载态**：来自 `dev-design-doc.md` 第 2.5 章
- **异常处理**：来自 `dev-design-doc.md` 第 7 章，后端错误码 + 前端展示

### 第三步：生成手动测试文档

按下方模板生成文档，包含完整的四个部分。

### 第四步：保存文档

**保存路径**：
- 情况 B / 空参数：`{features}/{name}/user-test/user-test-doc.md`
- 情况 A：文档所在目录的同级 `user-test/user-test-doc.md`

（若目录不存在，先创建）

保存后提示：
```
手动测试文档已保存至 {features}/{name}/user-test/user-test-doc.md
下一步：运行 /dev/run <功能名称> 开始开发
```

---

## 手动测试文档模板

````markdown
# [功能名称] 手动测试文档

> 生成时间：<日期>
> 基准文档：{features}/{name}/requirement/requirement-doc.md + {features}/{name}/dev-design/dev-design-doc.md
> 测试环境：<填写实际测试环境 URL>
> 测试人员：___________
> 测试日期：___________

---

## 第一部分：功能测试步骤

> 按功能点逐步操作验证，覆盖主流程和核心业务规则。

### P0 功能（必测）

#### <功能点名称>

| # | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 通过 |
|---|---------|---------|---------|---------|------|
| 1 | <满足什么条件> | <具体点击/输入操作> | <系统应该如何响应> | | ☐ |
| 2 | <前置条件> | <操作步骤> | <预期结果> | | ☐ |

#### <功能点名称>

| # | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 通过 |
|---|---------|---------|---------|---------|------|
| 1 | <前置条件> | <操作步骤> | <预期结果> | | ☐ |

### P1 功能（重要）

#### <功能点名称>

| # | 前置条件 | 操作步骤 | 预期结果 | 实际结果 | 通过 |
|---|---------|---------|---------|---------|------|
| 1 | <前置条件> | <操作步骤> | <预期结果> | | ☐ |

---

## 第二部分：边界与异常场景

> 验证系统在边界输入、异常数据、网络问题等场景下的处理是否符合规范。

### 数据边界

| # | 场景描述 | 操作步骤 | 预期结果 | 实际结果 | 通过 |
|---|---------|---------|---------|---------|------|
| 1 | 空数据 | <如：查询条件无匹配结果> | <如：显示空态提示"暂无数据"> | | ☐ |
| 2 | 最大值 | <如：输入超出允许范围的数值> | <如：显示校验错误提示> | | ☐ |
| 3 | 零值 | <如：分母为零的计算场景> | <如：显示"--"而非报错> | | ☐ |
| 4 | 负数 | <如：负值金额的展示> | <如：红色显示，前置"-"符号> | | ☐ |

### 错误输入

| # | 场景描述 | 操作步骤 | 预期结果 | 实际结果 | 通过 |
|---|---------|---------|---------|---------|------|
| 1 | 必填字段为空 | <提交时留空必填项> | <显示字段级错误提示> | | ☐ |
| 2 | 格式不正确 | <输入格式不符合要求的内容> | <显示格式校验提示> | | ☐ |
| 3 | 超出长度限制 | <输入超出最大长度的内容> | <截断或提示超出限制> | | ☐ |

### 权限与访问

| # | 场景描述 | 操作步骤 | 预期结果 | 实际结果 | 通过 |
|---|---------|---------|---------|---------|------|
| 1 | 无权限角色访问 | <使用无权限账号访问功能页面> | <跳转登录页或显示无权限提示> | | ☐ |
| 2 | Token 过期 | <等待 Token 过期后操作> | <自动跳转登录页> | | ☐ |

### 网络与系统异常

| # | 场景描述 | 操作步骤 | 预期结果 | 实际结果 | 通过 |
|---|---------|---------|---------|---------|------|
| 1 | 接口超时 | <模拟网络慢或断网后触发请求> | <显示加载超时提示，页面不崩溃> | | ☐ |
| 2 | 服务端报错 | <触发会产生 5xx 错误的操作> | <显示友好的错误提示，不展示原始错误> | | ☐ |

---

## 第三部分：UI 交互验证

> 对照设计文档第 2.4 章每条交互行为，逐项验证前端实现是否符合设计规范。

| # | 触发元素 | 触发方式 | 设计预期效果 | 实际效果 | 通过 |
|---|---------|---------|------------|---------|------|
| 1 | <如：新增按钮> | <点击> | <如：打开新增弹窗，标题为"新增XXX"> | | ☐ |
| 2 | <如：日期筛选器> | <选择日期后> | <如：列表数据自动刷新> | | ☐ |
| 3 | <如：表格行> | <点击行> | <如：跳转到详情页> | | ☐ |
| 4 | <如：删除按钮> | <点击> | <如：弹出确认对话框，确认后删除并刷新列表> | | ☐ |
| 5 | <如：分页控件> | <切换页码> | <如：加载对应页数据，URL 参数更新> | | ☐ |

### 数据展示格式验证

| # | 字段/场景 | 设计规范 | 实际展示 | 通过 |
|---|---------|---------|---------|------|
| 1 | 金额字段 | <如：保留2位小数，千分位分隔，前置$符号> | | ☐ |
| 2 | 负数显示 | <如：红色文字，数字前有"-"> | | ☐ |
| 3 | 空值处理 | <如：显示"--"而非空白或"null"> | | ☐ |
| 4 | 日期格式 | <如：YYYY-MM-DD> | | ☐ |
| 5 | 百分比格式 | <如：保留1位小数，后置"%"> | | ☐ |

### 加载与空态验证

| # | 场景 | 设计规范 | 实际展示 | 通过 |
|---|------|---------|---------|------|
| 1 | 数据加载中 | <如：表格区域显示 Spin 加载动画> | | ☐ |
| 2 | 查询无结果 | <如：显示空状态图标和提示文案"暂无数据"> | | ☐ |
| 3 | 接口报错 | <如：显示错误提示 Toast，不崩溃白屏> | | ☐ |

---

## 第四部分：验收检查清单

> 上线前由测试负责人逐项确认，全部通过后方可提测/上线。

### 功能完整性

- [ ] P0 功能点全部通过测试
- [ ] P1 功能点全部通过测试（或已知问题已记录）
- [ ] 所有业务流程可完整执行，无中断
- [ ] 数据计算结果与需求文档公式一致（已人工验算关键数值）

### 数据正确性

- [ ] 列表数据与数据库实际数据一致
- [ ] 筛选条件正确过滤数据
- [ ] 分页数据无重复、无遗漏
- [ ] 汇总计算结果正确（已与原始数据人工核对）

### 异常处理

- [ ] 所有边界场景有合理处理（不白屏、不崩溃）
- [ ] 必填字段缺失时有明确提示
- [ ] 网络错误时有友好提示
- [ ] 权限控制正常（未授权用户无法访问）

### UI 规范

- [ ] 数字格式符合设计规范（小数位、货币符号、千分位）
- [ ] 空值/null 统一显示为"--"（或设计规定的格式）
- [ ] 正负数颜色正确（正数绿色/黑色，负数红色）
- [ ] 加载态和空态均有实现
- [ ] 交互行为与设计文档第 2.4 章完全一致

### 性能与体验

- [ ] 页面首屏加载时间在可接受范围内（< 3秒）
- [ ] 大数据量下（>100条）列表无明显卡顿
- [ ] 操作响应及时，无明显延迟

---

## 测试总结

**测试日期**：___________
**测试人员**：___________
**测试环境**：___________

| 类别 | 总用例数 | 通过 | 失败 | 跳过 |
|------|---------|------|------|------|
| P0 功能测试 | | | | |
| P1 功能测试 | | | | |
| 边界/异常场景 | | | | |
| UI 交互验证 | | | | |
| 验收检查清单 | | | | |
| **合计** | | | | |

**是否通过验收**：☐ 通过  ☐ 不通过

**失败项说明**：
> <列出所有未通过的测试项及原因>

**遗留问题**：
> <不影响上线但需跟进的问题>
````

---

## 规则

- 每个测试项必须包含「操作步骤 / 预期结果 / 实际结果（留空）/ 通过 ☐」四列
- 功能测试步骤必须对应 `requirement-doc.md` 第 3、4 章的每个 P0/P1 功能点和主流程
- 边界场景必须覆盖 `requirement-doc.md` 第 5.4 章的全部异常规则
- UI 交互验证必须对照 `dev-design-doc.md` 第 2.4 章逐条列出，不得遗漏
- 验收清单中的计算公式验收项必须对应 `requirement-doc.md` 第 6 章的全部公式
- 所有占位符 `<...>` 必须替换为实际功能的具体内容
